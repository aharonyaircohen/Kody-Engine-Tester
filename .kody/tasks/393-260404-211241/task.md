# [test-suite] Add palindrome checker utility

Create a palindrome checker utility in src/utils/palindrome.ts that checks if a string is a palindrome (ignoring case, spaces, and punctuation). Include comprehensive unit tests in src/utils/palindrome.test.ts.