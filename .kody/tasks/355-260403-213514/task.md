# [test-suite] Add CORS middleware with configurable origins

Create a CORS middleware in src/middleware/cors-middleware.ts that supports configurable allowed origins, methods, and headers. Should handle preflight requests and include proper Access-Control headers. Include unit tests covering all configurations.