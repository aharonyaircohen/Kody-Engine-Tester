# [test-suite] Add array intersection utility

Create an intersection utility in src/utils/intersection.ts that returns the common elements between two arrays. Support options for custom comparator function and preserve order from the first array. Add tests in intersection.test.ts.