# [test-suite] Add string truncate utility function

Create a truncate utility function in src/utils/truncate.ts that truncates a string to a given length and appends an ellipsis. Include edge cases for empty strings, strings shorter than the limit, and custom suffix support. Add comprehensive tests in truncate.test.ts.