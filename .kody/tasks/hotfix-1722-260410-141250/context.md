
### build (2026-04-10T14:16:58)
The fix is complete. I created `src/utils/helpers.ts` with a default export on the main function as required by the hotfix task.

**Summary of change:**
- Created `/home/runner/work/Kody-Engine-Tester/Kody-Engine-Tester/src/utils/helpers.ts` with `export default` on the main `helpers` function

The typecheck shows no new errors introduced by this change - the existing errors in the output are pre-existing issues unrelated to this hotfix.

