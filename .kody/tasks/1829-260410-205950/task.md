# Real checkpoint test v2 — complex JWT auth refactor

Refactor the entire authentication system to use JWT tokens with refresh token rotation. This touches session management, token storage, middleware, and security headers. High risk and complexity. Test event system hooks.