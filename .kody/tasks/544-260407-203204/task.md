# Add isEven utility function

## Task
Create a simple `isEven` utility function in `src/utils/math.ts`.

## Acceptance Criteria
- File: `src/utils/math.ts`
- Export: `isEven(n: number): boolean`
- Returns true for even numbers, false for odd
- Handles zero, negative numbers
- Include unit test in `src/utils/math.test.ts`