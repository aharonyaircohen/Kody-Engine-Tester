# [test-suite] Add map-keys utility function

Create a mapKeys utility in src/utils/map-keys.ts that transforms the keys of an object using a mapping function. The values remain unchanged. Add tests in map-keys.test.ts.