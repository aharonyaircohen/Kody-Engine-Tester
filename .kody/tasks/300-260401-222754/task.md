# [test-suite] T13 - Add number clamp utility

Add a `clamp(value, min, max)` utility in `src/utils/clamp.ts` that clamps a number between min and max inclusive. Include TypeScript types and unit tests.