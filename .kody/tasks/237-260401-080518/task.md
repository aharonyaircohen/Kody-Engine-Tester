# [test-suite] Add sample utility function

Create a sample utility in src/utils/sample.ts that returns a random element from an array, or N random elements without replacement. Add tests in sample.test.ts.