# [test-suite] Add base64 encode/decode utility

Create a base64 encode/decode utility function in src/utils/base64.ts that supports both string and buffer inputs, with URL-safe encoding option. Include comprehensive unit tests.