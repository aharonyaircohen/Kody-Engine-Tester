# feat: [run-20260409-1749] T01: Simple utility function

## What

Create src/utils/queryBuilder.ts that builds URL-encoded query strings from key-value objects. Must handle arrays by repeating the key (e.g., ?key=1&key=2), empty objects, and nested objects (stringified). Full test coverage in src/utils/queryBuilder.test.ts.

## Scope

- `src/utils/queryBuilder.ts`
- `src/utils/queryBuilder.test.ts`

**Type:** feature | **Risk:** low
**Verify:** ✅ typecheck + tests + lint passed

Closes #928

## Rollback Plan

If this change causes problems:
1. **Quick revert:** `git revert <commit> && git push`
2. **Full rollback:** switch back to the previous release tag
3. **Database migration:** check if any migration is reversible — if not, backfill data before deploying this change

---
🤖 Generated by Kody

---

## Discussion (8 comments)

**@aharonyaircohen** (2026-04-09):
@kody review

**@aharonyaircohen** (2026-04-09):
## 🔍 Kody Review (`review-pr-932-260409-184457`)

## Verdict: PASS

## Summary

Created `src/utils/queryBuilder.ts` — a URL-encoded query string builder that handles primitives, arrays (repeated keys), and nested objects (stringified), with 12 passing tests in `src/utils/queryBuilder.test.ts`.

## Findings

### Critical
None.

### Major
None.

### Minor
None.

---

## Two-Pass Review

**Pass 1 — CRITICAL:**

### SQL & Data Safety
None — no database operations.

### Race Conditions & Concurrency
None — pure function, no state.

### LLM Output Trust Boundary
None — no LLM integration.

### Shell Injection
None — no shell execution.

### Enum & Value Completeness
Not applicable — no enums introduced.

**Pass 2 — INFORMATIONAL:**

### Conditional Side Effects
None — function is pure with no side effects.

### Test Gaps
None — 12 tests cover: empty objects, special character encoding, array repetition, null/undefined skipping, nested object stringification, booleans, numbers, empty arrays, and mixed types.

### Dead Code & Consistency
None — all code is used, JSDoc comments are accurate.

### Crypto & Entropy
None — no cryptographic operations.

### Performance & Bundle Impact
None — minimal footprint, no heavy dependencies.

### Type Coercion at Boundaries
None — `Record<string, unknown>` input properly handled with `String()` coercion for values and `JSON.stringify()` for nested objects.

---

**Tests:** 12/12 passing  
**Lint:** Clean (no errors in changed files)  
**TypeScript:** Clean (no errors in changed files)


---
🤖 Generated by Kody

**@aharonyaircohen** (2026-04-09):
@kody review

**@aharonyaircohen** (2026-04-09):
@kody review

**@aharonyaircohen** (2026-04-09):
## 🔍 Kody Review (`review-pr-932-260409-185234`)

## Verdict: PASS

## Summary

A new utility function `queryBuilder` in `src/utils/queryBuilder.ts` builds URL-encoded query strings from objects, supporting simple key-value pairs, arrays (repeated key syntax), nested objects (JSON stringified), and null/undefined skipping. Full test coverage in `src/utils/queryBuilder.test.ts` with 12 passing tests.

## Findings

### Critical
None.

### Major
None.

### Minor
None.

---

## Two-Pass Review

**Pass 1 — CRITICAL:**

### SQL & Data Safety
N/A — no database operations.

### Race Conditions & Concurrency
N/A — pure function with no state mutation or concurrency.

### LLM Output Trust Boundary
N/A — no LLM output involved.

### Shell Injection
N/A — no shell commands.

### Enum & Value Completeness
N/A — no new enum values introduced.

**Pass 2 — INFORMATIONAL:**

### Conditional Side Effects
None — function has no side effects.

### Test Gaps
None — 12 tests cover all acceptance criteria:
- Simple key-value pairs
- Empty objects
- Special character encoding (spaces, `&`)
- Array key repetition (`tags=a&tags=b&tags=c`)
- Numeric array values
- Null/undefined skipping (both top-level and array items)
- Nested object stringification
- Boolean and numeric values
- Empty arrays
- Mixed types

### Dead Code & Consistency
None — implementation matches the specification exactly.

### Crypto & Entropy
N/A — no cryptographic operations.

### Performance & Bundle Impact
Minimal — pure TypeScript utility, no heavy dependencies added.

### Type Coercion at Boundaries
Safe — `String(value)` coercion is intentional for converting primitives to their string representation for URL encoding. `JSON.stringify` handles nested objects correctly.

---

**Review complete.** This is a low-risk, well-tested utility function implementing URL query string building per the task specification. No issues found.


---
🤖 Generated by Kody

**@github-actions** (2026-04-09):
@kody fix

**@aharonyaircohen** (2026-04-09):
@kody fix\nPlease use "quotes" and handle $(dollar) signs

**@aharonyaircohen** (2026-04-09):
🚀 Kody pipeline started: `fix\nPlease` ([logs](https://github.com/aharonyaircohen/Kody-Engine-Tester/actions/runs/24208696305))

To rerun: `@kody rerun fix\nPlease --from <stage>`

