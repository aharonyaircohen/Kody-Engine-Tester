# [test-suite] Add binary search (bisect) utility

Create a bisect utility in src/utils/bisect.ts that performs binary search on sorted arrays. Functions: bisectLeft(arr, value), bisectRight(arr, value), insort(arr, value). Support custom comparator. Add tests in bisect.test.ts.