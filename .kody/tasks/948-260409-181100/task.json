```json
{
  "task_type": "feature",
  "title": "Add /login and /register HTTP endpoints",
  "description": "Create Next.js route handlers for POST /register and POST /login that expose existing business logic from src/api/auth/. The register endpoint should accept email/password, create user via Payload, validate passwords, return 409 on duplicate email, and return JWT tokens. The login endpoint should validate credentials via UserStore, return JWT tokens on success, and handle errors (400/401/403/423). Both should follow existing route handler patterns like src/app/api/notifications/route.ts using getPayloadInstance() and proper JSON error responses.",
  "scope": [
    "src/app/api/auth/login/route.ts",
    "src/app/api/auth/register/route.ts"
  ],
  "risk_level": "medium",
  "existing_patterns": [
    "src/app/api/notifications/route.ts - route handler pattern with getPayloadInstance() and JSON responses",
    "src/api/auth/register.ts - existing register business logic with validation and AuthService",
    "src/api/auth/login.ts - existing login business logic with UserStore, SessionStore, JwtService",
    "src/auth/auth-service.ts - JWT token generation via JwtService.signAccessToken/signRefreshToken",
    "src/auth/jwt-service.ts - token signing with 15min access token expiry and 7 day refresh token expiry"
  ],
  "questions": []
}
```
