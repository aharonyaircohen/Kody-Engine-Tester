# [test-suite] T11 - Add number formatting utility

Add a `formatNumber(n, options)` utility in `src/utils/format-number.ts` that formats numbers with locale-aware thousands separators and configurable decimal places. Include TypeScript types and unit tests.