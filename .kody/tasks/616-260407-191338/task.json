```json
{
  "task_type": "refactor",
  "title": "Refactor auth system with JWT and RBAC migration",
  "description": "Replace session-based auth with JWT-only validation, migrate user schema to include roles array, add proper RBAC with admin/editor/viewer roles, and update all dependent API routes. Requires creating migration script for existing user data and handling role mapping from legacy roles.",
  "scope": [
    "src/auth/session-store.ts",
    "src/auth/user-store.ts",
    "src/auth/withAuth.ts",
    "src/auth/_auth.ts",
    "src/auth/auth-service.ts",
    "src/auth/jwt-service.ts",
    "src/middleware/auth-middleware.ts",
    "src/api/auth/login.ts",
    "src/auth/index.ts"
  ],
  "risk_level": "high",
  "existing_patterns": [
    "src/auth/auth-service.ts:65-149 - AuthService.login() using Payload CMS with JWT tokens",
    "src/auth/jwt-service.ts - JWT signing/verification with HMAC-SHA256",
    "src/auth/_auth.ts:17-21 - ROLE_HIERARCHY constant with admin>editor>viewer levels",
    "src/auth/_auth.ts:37-61 - checkRole() function for role permission checks",
    "src/auth/withAuth.ts:55-108 - withAuth HOC wrapping route handlers with JWT validation"
  ],
  "questions": [
    "Should the old UserStore (SHA-256) be deprecated entirely in favor of AuthService (PBKDF2), or should they coexist during a transition period?",
    "Should the migration script map the old roles (student/instructor) to new roles (viewer/editor) or should there be a new 'instructor' role added to RbacRole?",
    "Should existing sessions in session-store.ts be invalidated immediately upon migration, or should there be a grace period for token refresh?"
  ]
}
```
