# [test-suite] Decompose: simple task falls back

Add a string truncation utility function in src/utils/truncate-words.ts that truncates a string to a maximum number of words, adding '...' if truncated. Include tests in src/utils/truncate-words.test.ts.