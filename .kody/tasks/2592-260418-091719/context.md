
### taskify (2026-04-18T09:21:28)
I've asked a clarifying question — the session is sandboxed to only the task directory and I cannot reach the `src/` files described in the task. Please let me know where the codebase lives so I can explore it before writing the plan.

### build (2026-04-18T09:25:32)
The plan is submitted for your approval. Once you approve, I'll implement all 6 files in order:

1. Install `zod`
2. Create validation schemas + unit tests
3. Create login route + tests
4. Create register route + tests
5. Run `pnpm tsc --noEmit` and `pnpm test:int` to verify
