# [test-suite] Add request-logger middleware with tests

Add a `request-logger` middleware that logs incoming HTTP requests with method, path, status code, and response time in milliseconds. Should support configurable log levels and output formats. Include unit tests.