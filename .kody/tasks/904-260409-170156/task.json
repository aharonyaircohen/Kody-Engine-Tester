{
  "task_type": "chore",
  "title": "Verify existing implementation of bad-types.ts",
  "description": "File src/utils/bad-types.ts already exists with the function `getCount(): string { return 42 }` which intentionally has a wrong return type (returns number 42 but declares string return type) to trigger typecheck failure. Task is complete.",
  "scope": ["src/utils/bad-types.ts"],
  "risk_level": "low",
  "existing_patterns": [],
  "questions": []
}
