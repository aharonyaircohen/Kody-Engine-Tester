# [test-suite] Add a number formatting utility

Add a number formatting utility in src/utils/format-currency.ts that formats numbers as currency strings with locale support (USD, EUR, etc). Add tests.