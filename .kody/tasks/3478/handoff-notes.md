Resolved a single symmetric merge conflict in `.github/workflows/kody.yml`. Both sides modified the same `run:` directive: the PR branch pinned to `0.4.108` and the dev branch used `@latest` with conditional chat/ci branching. Took the PR side (pinned version) since the PR's intent is adding stats utilities, not changing the kody workflow, and the pinned version reflects a deliberate stability choice from recent commit 8f55c80d. YAML syntax validated, no conflict markers remain.