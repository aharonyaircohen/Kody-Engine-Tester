# Add capitalize utility function

## Task
Add a `capitalize` utility function in `src/utils/capitalize.ts` that capitalizes the first letter of a string.

## Acceptance Criteria
- Export: `export function capitalize(str: string): string`
- Capitalizes the first character, lowercases the rest
- Returns empty string for empty input
- Include tests in `src/utils/capitalize.test.ts`