```json
{
  "task_type": "feature",
  "title": "Add User domain model with password hash field",
  "description": "Create a User domain model in src/models/user.ts with fields: id, email, passwordHash, createdAt, updatedAt. The model should be a TypeScript interface exported for use by other auth modules. Unit tests should verify password hashing produces unique hashes per user (due to salt) and model type correctness. Integration test should verify creating a user persists hashed password, not plain text.",
  "scope": [
    "src/models/user.ts",
    "src/models/user.test.ts"
  ],
  "risk_level": "low",
  "existing_patterns": [
    "src/models/notification.ts - domain model pattern with TypeScript interfaces, single-function module files with co-located tests",
    "src/auth/user-store.ts:5-16 - existing User interface with passwordHash and salt fields",
    "src/auth/auth-service.ts:45-60 - PBKDF2 password verification using crypto.timingSafeEqual for timing-attack safety"
  ],
  "questions": [
    "The existing Users Payload collection uses Payload's built-in auth with hash/salt fields, while UserStore uses passwordHash/salt. Should this new model use passwordHash naming (intentionally different from Payload's hash) to clarify it's a separate domain model, or align with Payload's hash field name?"
  ]
}
```
