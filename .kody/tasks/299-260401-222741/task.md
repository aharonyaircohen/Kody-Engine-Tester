# [test-suite] T14 - Add dark mode toggle button component

Add a `DarkModeToggle` React component in `src/components/dark-mode-toggle.tsx` that toggles between light and dark themes. Should use a button with a sun/moon icon, persist preference to localStorage, and update the `data-theme` attribute on the HTML element. Include unit tests with `@testing-library/react`.