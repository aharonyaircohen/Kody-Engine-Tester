# Add isEven utility function

## Task
Add an `isEven` utility function in `src/utils/is-even.ts`.

## Acceptance Criteria
- Export: `export function isEven(n: number): boolean`
- Returns true for even numbers, false for odd
- Include tests in `src/utils/is-even.test.ts`