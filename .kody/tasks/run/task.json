{
"task_type": "feature",
"title": "Add truncateText utility function",
"description": "Add a truncateText(text: string, maxLength: number): string function that truncates text and appends '...' if it exceeds maxLength, with comprehensive unit tests covering truncation, edge cases (empty string, maxLength <= 0, unicode characters).",
"scope": ["src/utils/truncateText.ts", "src/utils/truncateText.test.ts"],
"risk_level": "low",
"hasUI": false,
"questions": []
}
