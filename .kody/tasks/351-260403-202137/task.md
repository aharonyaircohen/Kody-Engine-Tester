# feat: Add a formatDate utility function

Add a `formatDate(date: Date, format: string): string` utility function to `src/utils/`.

Requirements:
- Support formats: `YYYY-MM-DD`, `MM/DD/YYYY`, `DD MMM YYYY`
- Export from `src/utils/index.ts`
- Add unit tests