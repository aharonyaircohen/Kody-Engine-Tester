# [test-suite] Add array partition utility

Create a partition utility in src/utils/partition.ts that splits an array into two groups based on a predicate function. The first group contains elements that match the predicate, the second contains those that don't. Support async predicates. Add comprehensive tests in partition.test.ts.