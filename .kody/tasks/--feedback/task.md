# [test-suite] Flag: feedback injection

Create a priority queue utility in src/utils/priority-queue.ts that supports enqueue with priority, dequeue, and peek operations. Add tests in priority-queue.test.ts.