```json
{
  "task_type": "feature",
  "title": "Add withCache wrapper function",
  "description": "Create `withCache<T>(key: string, ttlMs: number, fn: () => Promise<T>): Promise<T>` in `src/services/cache-wrapper.ts` using in-memory Map with TTL expiry. The wrapper should check cache first, return cached value if valid, otherwise call the function and cache the result. Include tests in `src/services/cache-wrapper.test.ts`.",
  "scope": ["src/services/cache-wrapper.ts", "src/services/cache-wrapper.test.ts"],
  "risk_level": "low",
  "existing_patterns": [
    "src/utils/cache.ts - Cache class with TTL expiry using Map; provides the underlying TTL-in-Map pattern",
    "src/utils/memoize.ts - memoization wrapper pattern returning a wrapped function with clear() method",
    "src/services/*.test.ts - co-located test files with .test.ts suffix"
  ],
  "questions": []
}
```
