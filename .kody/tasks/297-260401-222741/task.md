# [test-suite] T10 - Add simple string truncate utility

Add a simple `truncate(str, maxLen, suffix='...')` utility function in `src/utils/truncate.ts` that truncates a string to maxLen characters and appends the suffix if truncated. Include TypeScript types and unit tests.