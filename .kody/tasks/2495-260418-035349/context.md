
### taskify (2026-04-18T03:59:32)
The plan is ready for your review. Once you approve, I'll move into implementation (adding the fields to `Users.ts`, creating `password-hash.ts`, refactoring `auth-service.ts`, and adding the tests).

### build (2026-04-18T04:01:33)
The plan is written and awaiting your review. Once you approve it, I'll move straight into implementation — adding the two fields to `Users.ts`, creating the `password-hash.ts` utility, wiring it into `AuthService`, and adding the tests.
