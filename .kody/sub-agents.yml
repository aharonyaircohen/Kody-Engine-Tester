# Folder-scoped sub-agents
# Generated by Kody bootstrap
# User can edit this file to customize agents

src-agent:
  scope: 'src/'
  model: haiku
  instructions: |

    - Use absolute imports: @/services/progress, @/components/dashboard/CourseProgressCard, @/components/dashboard/UpcomingDeadlines

    - Large module: consider splitting files >300 lines

    Isolation:
    - Only modify files within this scope
    - Do NOT import from other scope folders directly
