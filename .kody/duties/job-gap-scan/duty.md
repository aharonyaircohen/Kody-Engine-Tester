# Job Gap Scan

Once a day, propose one high-ROI job the system does not yet have. Read `.kody/memory/` first, skip rejected or recently dismissed ideas, and open at most one advisory GitHub issue labeled `kody:ceo-proposal`.

The implementation reference is `.kody/scripts/job-gap-scan-tick.py`. The proposal is advisory only; do not write a new job file directly.
