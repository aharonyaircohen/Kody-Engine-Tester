The chunk tests pass (8/8). The only failure is an unrelated `api.int.spec.ts` test that requires a Payload CMS secret key — not related to this task.

Both `src/utils/chunk.ts` and `src/utils/chunk.test.ts` are already implemented and all 8 chunk tests pass.
