# Add a chunk utility function

Create a utility function called chunk that takes an array and a size number, returns an array of arrays each with at most size elements. Save to src/utils/chunk.ts with tests in src/utils/chunk.test.ts.