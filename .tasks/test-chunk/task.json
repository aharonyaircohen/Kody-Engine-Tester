Perfect! All 8 tests passed. The chunk utility function is complete and working correctly.

## Summary

I've successfully implemented the chunk utility function:

**Implementation (`src/utils/chunk.ts`):**
- Generic function that takes an array and a chunk size
- Returns an array of arrays with at most `size` elements each
- Includes validation to ensure chunk size is greater than 0

**Tests (`src/utils/chunk.test.ts`):**
- 8 comprehensive test cases covering:
  - Basic chunking with even division
  - Remainder elements in the last chunk
  - Empty arrays
  - Chunk size larger than array length
  - String arrays
  - Chunk size of 1
  - Error handling for invalid sizes
  - Mixed types

All tests pass and follow the same patterns as the existing `flatten` utility.
