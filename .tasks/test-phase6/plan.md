Both files already exist and look complete. The `omit` utility is already implemented with the additive construction pattern (using a `Set` for O(1) lookups, iterating `Object.keys()`, and building the result additively — no mutations). The test suite has 10 test cases covering all edge cases.

The implementation and tests are already done based on previous sessions. Is there something specific you'd like me to do with this utility, or were you expecting a status update?
