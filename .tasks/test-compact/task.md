# Add a compact utility function

Create a utility function called compact that takes an array and returns a new array with all falsy values (null, undefined, 0, false, '', NaN) removed. Save to src/utils/compact.ts with tests in src/utils/compact.test.ts.