# Add a unique utility function

Create a utility function called unique that takes an array and returns a new array with duplicate values removed. Should work with primitive types (strings, numbers). Save to src/utils/unique.ts with tests in src/utils/unique.test.ts.