name: CI Repair Verification

on:
  workflow_dispatch:

jobs:
  controlled-failure:
    runs-on: ubuntu-latest
    steps:
      - name: Verify CI repair can restore a red check
        run: |
          echo "This controlled check is red for the CI Repair end-to-end test."
          exit 1
