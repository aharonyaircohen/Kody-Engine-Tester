# Kody workflow chain live proof

This harmless fixture verifies that a successful **CI Repair** workflow can
start the reusable **Review and Merge** workflow through a repository trigger.

The trigger is owned by Kody Dashboard configuration. The consumer repository
does not need a custom GitHub Actions workflow for this chain.
