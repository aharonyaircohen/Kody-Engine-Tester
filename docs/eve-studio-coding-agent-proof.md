# Eve Studio coding-agent proof

This change was produced by the Eve Studio pipeline.

The four stages are:

1. **classifier** — triages the work item
2. **analyst** — writes acceptance criteria and implementation plan
3. **implementer** — writes the code on a feature branch
4. **reviewer** — reviews the diff against acceptance criteria

The final output is a draft pull request.
